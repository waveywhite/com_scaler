<?xml version="1.0" encoding="UTF-8"?>
<metadata>
	<layout title="COM_SCALER_IMAGE_VIEW_DEFAULT_TITLE">
		<message>COM_SCALER_IMAGE_VIEW_DEFAULT_DESC</message>
	</layout>
	<!-- Add fields to the request variables for the layout. -->
	<fields name="request">
		<fieldset name="request">
			<field
				name="format"
				type="hidden"
				default="raw"
			/>
		</fieldset>
	</fields>

	<!-- Add fields to the parameters object for the layout. -->
	<fields name="params">

	</fields>
</metadata>